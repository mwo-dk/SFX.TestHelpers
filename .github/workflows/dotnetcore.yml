name: .NET Core

on: [pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.0.100
    - name: Build 
      run: dotnet build --configuration Release
    - name: Build NuGet Package
      run: dotnet pack ./src/TestHelpers/TestPropers.fsproj --configuration Release -o .Nuget
    - name: Deploy NuGet Package
      run: dotnet nuget push ./src/TestHelpers/.Nuget/TestHelpers.2.2.1.nupkg -k $NUGET_API_KEY -s https://api.nuget.org/v3/index.json

